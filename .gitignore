# Python
__pycache__/
*.pyc

# Excel lock files
~$*.xlsx

# Backup / scratch copies (not part of the pipeline)
*- Copy*.xlsx
*_backup_*.xlsx
out*.txt
headers*.txt
row_out.txt
rows_out.txt
scratch_out*.json
scratch_out*.txt

# Machine-local Claude Code config (contains absolute local paths)
.claude/settings.local.json

# Everything below runs/lives locally only. This repo publishes the dashboard,
# not the research/scoring pipeline that builds it.
/data/
/doc/
/scripts/
/dashboard/index.html

# Local launchers, moved to the repo root 2026-07-31 so they're easy to
# double-click. Same reasoning as /scripts/ above -- they drive the local
# pipeline and hard-code local absolute paths, so they don't belong in a
# dashboard-only repo.
/*.bat
/bat_readme.md
